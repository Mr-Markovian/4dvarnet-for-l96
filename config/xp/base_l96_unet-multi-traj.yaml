# @package _global_
# This referes to Vanilla Styled 4dvarnet with  periodic BCs in convs. It has observation noise of 0.04

paths:
    #l96: /Odyssey/public/l96_dataset/L96_truth_obs_mask_boc2010.nc
    l96_dir: /Odyssey/public/l96_dataset/
    l96_trajectories: 
      - ${paths.l96_dir}/L96_truth_obs_mask_boc2010_single_traj_1.nc
      - ${paths.l96_dir}/L96_truth_obs_mask_boc2010_single_traj_2.nc
      - ${paths.l96_dir}/L96_truth_obs_mask_boc2010_single_traj_3.nc
      - ${paths.l96_dir}/L96_truth_obs_mask_boc2010_single_traj_4.nc
    
domain: ???

trainer:
  _target_: pytorch_lightning.Trainer
  inference_mode: False
  gradient_clip_val: 0.5
  accelerator: gpu
  devices: 1
  logger: 
    _target_: pytorch_lightning.loggers.CSVLogger
    save_dir: ${hydra:runtime.output_dir}
    name: ${hydra:runtime.choices.xp}
    version: ''
  max_epochs: 500
  callbacks:
    - _target_: pytorch_lightning.callbacks.LearningRateMonitor
    - _target_: pytorch_lightning.callbacks.ModelCheckpoint
      monitor: val_mse
      save_top_k: 3
      filename: '{val_mse:.5f}-{epoch:03d}'

datamodule:
  _target_: contrib.L96.data.MultiTrajDataModule
  input_das: 
    _target_: contrib.L96.utils.load_l96_data_multi
    paths: ${paths.l96_trajectories} 
  domains:
    train:
      time: {_target_: builtins.slice, _args_: [20000, 80000]}
    val: 
      time: {_target_: builtins.slice, _args_: [11000, 19000]}
    test: 
      time: {_target_: builtins.slice,  _args_: [1000, 10000]}
  xrds_kw:
    patch_dims: { time: 200, lat: 40, lon: 1}
    strides: { time: 50, lat: 40, lon: 1}
    domain_limits: ${domain.train}
  dl_kw: {batch_size: 32, num_workers: 2}
  aug_kw:
    aug_factor: 2
    aug_only: True
    noise_sigma: 0.03

model:
  _target_: contrib.L96.models.LitUnet_like_4dVarNet
  persist_rw: False
  opt_fn:
    _target_: contrib.L96.utils.cosanneal_lr_adam_UNet
    _partial_: true
    lr: 1e-4
    T_max: ${trainer.max_epochs}
  rec_weight:
      _target_: contrib.L96.utils.get_triang_time_wei
      patch_dims: ${datamodule.xrds_kw.patch_dims}
      crop: {time: 0, lat: 0, lon: 0}
      offset: 0
  solver: 
    _target_: contrib.L96.models.UNet
    n_channels: 1  # n_channels = n_obs_channels + n_init_channels
    n_classes: 1
    bilinear: True
    block:
      _target_: contrib.L96.utils.ResBlock_periodic
      _partial_: True
    add_input: True
    channel_dims: [32,64,128] #[16,32,64]
      
       
  test_metrics: ${metrics.test_metrics}
  pre_metric_fn: 
        _target_: xarray.Dataset.sel
        _partial_: True
        time: {_target_: builtins.slice, _args_: [1000, 10000]}
        lat: ${domain.test.lat}
        lon: ${domain.test.lon}

metrics:
  nrmse_scores: {_target_: ocean4dvarnet.utils.rmse_based_scores_from_ds, _partial_: True}
  psd_scores: {_target_: ocean4dvarnet.utils.psd_based_scores_from_ds, _partial_: True}
  get0: {_target_: operator.itemgetter, _args_: [0]}
  get1: {_target_: operator.itemgetter, _args_: [1]}
  test_metrics:
    'mu': {_target_: ocean4dvarnet.utils.pipe, _partial_: true, fns: ['${metrics.nrmse_scores}', '${metrics.get0}']}
    'sig': {_target_: ocean4dvarnet.utils.pipe, _partial_: true, fns: ['${metrics.nrmse_scores}', '${metrics.get1}']}
    'lx': {_target_: ocean4dvarnet.utils.pipe, _partial_: true, fns: ['${metrics.psd_scores}', '${metrics.get0}']}
    'lt': {_target_: ocean4dvarnet.utils.pipe, _partial_: true, fns: ['${metrics.psd_scores}', '${metrics.get1}']}

entrypoints:
  - _target_: pytorch_lightning.seed_everything
    seed: 333
  - _target_: ocean4dvarnet.train.base_training
    trainer: ${trainer}
    lit_mod: ${model}
    dm: ${datamodule}

defaults:
  - /domain: l96_sim_40
  - _self_

